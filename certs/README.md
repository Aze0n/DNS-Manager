lege hier cert.pem und key.pem ab.

- cert.pem: zertifikat (kette optional)
- key.pem: privater schlüssel (passt zum cert)

alternativ: ohne dateien startet der container mit self-signed (außer STRICT_TLS=1).